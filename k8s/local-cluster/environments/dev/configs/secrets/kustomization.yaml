namespace: dev
resources:
  # === REAL AWS SECRETS MANAGER INTEGRATION ===
  - aws-credentials.yaml
  - clustersecretstore.yaml
  - aws-consumer-externalsecret.yaml  
  - aws-airflow-externalsecret.yaml
  - aws-postgres-externalsecret.yaml
  
  # === ДЛЯ ЛОКАЛЬНОГО DEVELOPMENT (отключено) ===
  # - local-secrets-setup.yaml
