apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: kafka-consumer-secret
  namespace: dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: kafka-consumer-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: dev-iwt-kafka-consumer-creds
